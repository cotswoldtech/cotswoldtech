extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " | "}

block description
  meta(name='description',
       content='#{page.title}, part of the #{locals.name} family')

block header
  h1= page.title
  p.description= page.metadata.tagline

block content
  article.article
    section.content!= typogr(page.html).typogrify()

block prepend footer
  div.nav
    a(href=contents.index.url) « Full listing
